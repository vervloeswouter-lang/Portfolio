/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
-  experimental: { typedRoutes: true }
+  // experimental: { typedRoutes: true } // disabled
};
export default nextConfig;
